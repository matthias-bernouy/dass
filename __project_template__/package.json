{
    "name": "my-application",
    "version": "0.0.1",
    "description": "my beautifull app",
    "scripts": {
        "bun:build": "bun build.ts",
        "generate-types": "tsc",
        
        "cli": "bun run src/cli/cli.ts",
        "dev": "nodemon --watch resources --watch src --ext ts,c,txt,json,h,md --clear --exec 'clear && bun run __project_template__/App.ts'"
    },
    "devDependencies": {
        "@types/bun": "latest",
        "bun-types": "^1.3.6",
        "dass": "link:dass"
    },
    "peerDependencies": {
        "typescript": "^5.9.3"
    }
}