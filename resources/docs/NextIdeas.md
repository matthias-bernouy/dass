


Manage the authentification and authorization (RBAC, ABAC, ReBAC) (JWT) (LOGINS PSRWD, AsyncKey, OpenID, SAML, ...)
Doing Transaction transparency for the end developers
Make the application decentralized to horizontal scaling
Create a research engine like db columns to get a very quickly research
Make schema more smart with basics conditionnals (min,max,regex...)
Make schema more smart with advanced conditionnals (calculated...)
Make schema more smart with advanced sharding like a product quantity which can dispatched between threads, nodes
ex: the new iphone has been released, 150 000 are sold each seconds, if there is only one thread responsible of this, it could be hard

Automatic API documentation
Automatic Client library in TS for the moment
MCP Option

Create TCP communication based on HTTP routes to reduce latency and data transfer.
Cold system for documents which are not call frequently -> save on disk, this functionality is to, store more documents on a single server with a minimal adding latency.
JS Macro to get a beautifull UX Dev but, in the build, it remove all classes and functions to inline FFI calls.
Rate limit configuration (by ip, by methods.....)